from django.db import models

# Replace this module with `inspectdb` output for the upstream GREGoR tables.
# Example workflow:
#   python manage.py inspectdb > gregor_app/models.py
#
# Once the models are in place, `gregor_app/admin.py` will auto-register them
# with a generic admin configuration so they are immediately visible in Django admin.
